schmema
  数据库创建脚本等
data
  初始化数据
update
  更新
update/old
  之前更新历史放着（简单的人工版本化）

命名规范：
  表名          表名
  主键名        pk_表名
  外键名        fk_当前表名_参照表名_列名
  非唯一索引名  idx_表名_列名
  唯一索引名    unique_表名_列名

因为sql中有触发器 所以此处需要使用;;里分隔不同的sql